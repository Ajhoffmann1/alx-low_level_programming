#include <stdio.h:>
#include <stdlib.h>
#include <time.h>

/**
 * main - entering point 
 * Return: just 0(success)
 */
int main(void)
{
	int r;
	char low;

	for (r = '0';  r <= 'f'; d++)
	purchar(d);

	for (low = 'a'; low <= 'f'; low++)
	purchar(low);
	printf("\n");

	return (0);
}
